const test=require('node:test'),assert=require('node:assert/strict');
const service=require('./report.service'),controller=require('./report.controller');
test('exportación responde binario con headers XLSX',async()=>{const original=service.exportReport;service.exportReport=async(type,query)=>{assert.equal(type,'sales-by-date');assert.equal(query.status,'completada');return{buffer:Buffer.from('xlsx'),filename:'liquorix_ventas_2026-08-18.xlsx'}};const headers={},res={setHeader:(key,value)=>{headers[key]=value},status(code){this.statusCode=code;return this},send(body){this.body=body;return this}},next=(error)=>{throw error};try{await controller.exportReport({params:{type:'sales-by-date'},query:{status:'completada'}},res,next);assert.equal(res.statusCode,200);assert.equal(headers['Content-Type'],'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet');assert.equal(headers['Content-Disposition'],'attachment; filename="liquorix_ventas_2026-08-18.xlsx"');assert.deepEqual(res.body,Buffer.from('xlsx'));}finally{service.exportReport=original;}});
